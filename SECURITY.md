# Security

Arrow Viewer is designed for local file inspection. The standalone HTML uses a restrictive CSP with `connect-src 'none'` and does not upload selected files.

Please avoid sharing real files with sensitive information in public bug reports; use synthetic reproductions where possible.
